<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.sergiobarriodelavega.xend">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChatActivity" />
        <activity android:name=".ProfileInfoActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProfileEditActivity" />
        <activity android:name=".ui.settings.StartSettingsActivity" />
        <activity
            android:name=".SetupWizardServerActivity"
            android:theme="@style/SetupWizardActivity" />
        <activity
            android:name=".SetupWizardLoginActivity"
            android:theme="@style/SetupWizardActivity" />
        <activity
            android:name=".SetupWizardMakeConnectionActivity"
            android:theme="@style/SetupWizardActivity" />
    </application>

</manifest>