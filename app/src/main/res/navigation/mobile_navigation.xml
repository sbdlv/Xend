<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_chats">

    <fragment
    android:id="@+id/nav_chats"
    android:name="me.sergiobarriodelavega.xend.ui.chats.ChatsFragment"
    android:label="@string/menu_chats"
    tools:layout="@layout/fragment_chats" />
    <fragment
        android:id="@+id/nav_contacts"
        android:name="me.sergiobarriodelavega.xend.ui.users.UsersFragment"
        android:label="@string/menu_contacts"
        tools:layout="@layout/fragment_users" />
    <activity
        android:id="@+id/nav_settings"
        android:label="@string/menu_contacts"
        android:name="me.sergiobarriodelavega.xend.ui.settings.StartSettingsActivity"
        />

</navigation>